[package]
name = "ehrmantraut"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ehrmantraut-core = { path = "../core" }
ehrmantraut-js-lowering = { path = "../js-lowering" }
napi = "3.0.0"
napi-derive = "3.0.0"

[build-dependencies]
napi-build = "2"
